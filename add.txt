new add
从现在起，只要本地作了提交，就可以通过命令：

$ git push origin master
把本地master分支的最新修改推送至GitHub，现在，你就拥有了真正的分布式版本库！
        for (int i = 0; i < totalDataArr.size(); i++) {
            JSONObject mObj = (JSONObject) totalDataArr.get(i);
        }
aa

bbc

this is my {
   var a = getS();
}


    for (int i = 0; i < totalDataArr.size(); i++) {
        JSONObject mObj = (JSONObject) totalDataArr.get(i);

        String gcdh = mObj.getString("ProjectOrderIdMaster");          //工程单号
        String ddts = mObj.getString("SolutionStatus");          //订单套数
        String ddh = mObj.getString("orderid");                 //ishare订单号
        String xsdh = mObj.getString("SalesOrderNumber");         //销售单号
        String hxm = mObj.getString("LineItem");                //行项目
        String hxfah = mObj.getString("Designer");               //户型方案号
        String bjzs = mObj.getString("OrderSumCnt");            //板件总数
        String klzs = mObj.getString("KLSumCnt");               //开料总数
        String klwcsl = mObj.getString("KLScanCnt");            //开料完成数量
        String klkssj = mObj.getString("KLStartTime");          //开料开始时间
        String klwcsj = mObj.getString("KLEndTime");            //开料完成时间
        String fbzs = mObj.getString("FBSumCnt");               //封边总数
        master edit aa row

   }

   dev edit aa



